#!/usr/bin/env perl
use App::Cope;

sub process {
  line qr{^((?:....:)?..:..\..)\s([^:]+:|"[^"]+")} => 'cyan', 'cyan bold';
  mark qr{\(rev \d+\)} => 'black bold';
}

chomp( my $realpath = `which lspci` );
run( \&process, $realpath, @ARGV );
